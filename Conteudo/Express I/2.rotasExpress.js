// Sistema de roteamento do Express:

// app.METHOD(PATH, HANDLER);
// Method = Método HTTP
// Path = Nome do caminho
// Handler = Função a ser executada

// Sempre que mudamos alguma coisa no app.js, devemos parar o terminar e executar de novo
// Para evitar isso, podemos instalar o nodemon no terminal
// Esse nodemon atualizará tudo que você mudou no 'app.js' automaticamente após salvar as alterações